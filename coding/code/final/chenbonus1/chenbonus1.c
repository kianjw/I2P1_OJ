#include <stdio.h>
int main(){
    int t;
    int n, q;
    scanf("%d", &t);
    while(t--){
        scanf("%d%d", &n, &q);
        while(q--){
            int l, r;
            scanf("%d%d",&l,&r);
            
        }
    }
}